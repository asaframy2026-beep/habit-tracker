# My Daily Companion (Mobile)

متتبع العادات موبيل - A Flutter-based personal habit tracker and productivity companion.

## Setup
1. Ensure Flutter is installed.
2. Run `flutter pub get`.
3. Run `flutter run`.
